//
//  {{ module_info.name | capitalize }} module - {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit
import RxSwift
import RxCocoa
import SnapKit

final class {{ module_info.name | capitalize }}View: UIView {
    // MARK: - Private properties
    
    // MARK: - Init
    override init(frame: CGRect) {
        super.init(frame: frame)
        setupView()
    }
    
    @available(*, unavailable)
    required init?(coder: NSCoder) { nil }
    
    // MARK: - SetupView
    private func setupView() {
        
    }
}

private extension {{ module_info.name | capitalize }}View {
    func configure(model: Model) {
        
    }
}

extension {{ module_info.name | capitalize }}View {
    struct Model {
        
    }
}

extension Reactive where Base == {{ module_info.name | capitalize }}View {
    var model: Binder<Base.Model> {
        Binder(base) { $0.configure(model: $1)}
    }
}

