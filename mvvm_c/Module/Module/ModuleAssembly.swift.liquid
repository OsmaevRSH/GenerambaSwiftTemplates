//
//  {{ module_info.name }} module - {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import Swinject

final class {{ module_info.name }}ModuleAssembly: Assembly {
    func assemble(container: Container) {
        container.register({{ module_info.name }}Module.self) { _ in
            let useCase = {{ module_info.name }}UseCaseImpl()
            let viewModel = {{ module_info.name }}ViewModel(useCase: useCase)
            let controller = {{ module_info.name }}ViewController(
                viewModel: viewModel
            )
            
            return {{ module_info.name }}Module(
                view: controller,
                output: viewModel.output.coord,
                input: viewModel.input.coord
            )
        }
    }
}
