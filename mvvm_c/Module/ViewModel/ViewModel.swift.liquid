//
//  {{ module_info.name }} module - {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import RxSwift
import RxCocoa

final class {{ module_info.name }}ViewModel {
    
    let input = Input()
    var output: Output
    
    init(
        useCase: {{ module_info.name }}UseCase
    ) {
        let fromView = input.view
        
        let view = Output.View()
        let coord = Output.Coord()
        
        output = .init(view: view, coord: coord)
    }
}

extension {{ module_info.name }}ViewModel {
    struct Input {
        let view = View()
        let coord = Coord()
        
        struct View {}
        class Coord: {{ module_info.name }}ModuleInput {}
    }
    
    struct Output {
        var view: View
        var coord: Coord
        
        struct View {}
        class Coord: {{ module_info.name }}ModuleOutput {}
    }
}
