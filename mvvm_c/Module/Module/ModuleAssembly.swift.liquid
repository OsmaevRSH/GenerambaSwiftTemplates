//
//  {{ module_info.name | capitalize }} module - {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import Swinject

final class {{ module_info.name | capitalize }}ModuleAssembly: Assembly {
    func assemble(container: Container) {
        container.register({{ module_info.name | capitalize }}Module.self) { _ in
            let useCase = {{ module_info.name | capitalize }}UseCaseImpl()
            let viewModel = {{ module_info.name | capitalize }}ViewModel(useCase: useCase)
            let controller = {{ module_info.name | capitalize }}ViewController(
                viewModel: viewModel
            )
            
            return {{ module_info.name | capitalize }}Module(
                view: controller,
                output: viewModel.output.coord,
                input: viewModel.input.coord
            )
        }
    }
}
